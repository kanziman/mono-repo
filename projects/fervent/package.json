{
  "name": "fervent",
  "version": "1.0.0",
  "description": "Standardized development harness and configurations for Fervent projects.",
  "main": "index.js",
  "scripts": {
    "format": "prettier --write .",
    "lint": "eslint . && prettier --check .",
    "test": "echo \"Running tests...\" && exit 0"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "prettier": "^3.0.0"
  }
}
